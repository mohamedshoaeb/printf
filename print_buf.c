#include "main.h"
/**
 * print_buf - prints buffer
 *
 * @buf: buffer we want print
 * @nbuf: number of bits to print
 *
 * Return : number of bits printed
 */

int print_buf(char *buf, int nbuf)
{
	return (write(1, buf, nibuf));
}
